#include <cstdlib>
#include <string>
#include <sstream>
#include <fstream>

using namespace std;

int main() {
	while(1) {
		system("echo \"Hello\" > /dev/ttyAMA0 \n");
	}
	return 0;
}
string call_line = "echo \"" + pd + "," + pa0 + "," + pa1 + "," + pc + "\n\" > /dev/ttyAMA0 \n";
                   // call_line << "echo \"" << pd << "\n\" > /dev/ttyAMA0" << endl;
                    system(call_line.c_str());

